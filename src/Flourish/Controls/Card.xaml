<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="clr-namespace:ArkheideSystem.Flourish.Controls"
>

  <Style TargetType="{x:Type controls:FlourishCard}">
    <Setter Property="Padding" Value="{DynamicResource FlourishCardPadding}" />
    <Setter Property="Background" Value="{DynamicResource FlourishCardBackgroundBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource FlourishSurfaceStrokeBrush}" />
    <Setter Property="BorderThickness" Value="{DynamicResource FlourishSurfaceBorderThickness}" />
    <Setter Property="Foreground" Value="{DynamicResource FlourishNeutralForeground1Brush}" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:FlourishCard}">
          <Grid>
            <Border
              x:Name="ShadowChrome"
              Background="{DynamicResource FlourishNeutralForeground1Brush}"
              CornerRadius="{DynamicResource FlourishSurfaceCornerRadius}"
              IsHitTestVisible="False"
              Visibility="Collapsed"
            />
            <Border
              x:Name="SurfaceChrome"
              Padding="{TemplateBinding Padding}"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius="{DynamicResource FlourishSurfaceCornerRadius}"
              SnapsToDevicePixels="True"
            >
              <ContentPresenter
                x:Name="ContentHost"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
              />
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="Appearance" Value="Subtle">
              <Setter Property="Background" Value="{DynamicResource FlourishNeutralBackground2Brush}" />
            </Trigger>
            <Trigger Property="Appearance" Value="Accent">
              <Setter Property="Background" Value="{DynamicResource FlourishAccentSurfaceBrush}" />
            </Trigger>
            <Trigger Property="Appearance" Value="Elevated">
              <Setter TargetName="ShadowChrome" Property="Effect" Value="{DynamicResource FlourishElevation1Effect}" />
              <Setter TargetName="ShadowChrome" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="Appearance" Value="Hero">
              <Setter Property="Padding" Value="{DynamicResource FlourishHeroCardPadding}" />
              <Setter Property="Background" Value="{DynamicResource FlourishHeroBackgroundBrush}" />
              <Setter TargetName="ShadowChrome" Property="Effect" Value="{DynamicResource FlourishElevation2Effect}" />
              <Setter TargetName="ShadowChrome" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.62" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>

<Page
  x:Class="ArkheideSystem.Gallery.Views.ChunkPage"
  x:Name="RootPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:flourish="http://schemas.arkheide.system/flourish"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="Transparent"
  SnapsToDevicePixels="True"
  UseLayoutRounding="True"
>
  <flourish:ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="{DynamicResource FlourishPageContentMargin}">
      <flourish:ChunkHero
        x:Name="HeroPreview"
        ChunkHeroDescription="Compose Gallery pages from focused sections."
        ChunkHeroTitle="Chunk"
        PresenterMode="Split"
        PresenterPosition="Right"
      >
        <flourish:ChunkHero.ChunkHeroBody>
          <WrapPanel>
            <flourish:Button Variant="Filled" Click="PresenterRight_Click" Content="Presenter right" />
            <flourish:Button Margin="8,0,0,0" Variant="Tonal" Click="PresenterLeft_Click" Content="Presenter left" />
            <flourish:Button Margin="8,0,0,0" Variant="Tonal" Click="Overlay_Click" Content="Overlay" />
          </WrapPanel>
        </flourish:ChunkHero.ChunkHeroBody>
        <flourish:ChunkHero.Presenter>
          <Border MinHeight="220" Background="{DynamicResource FlourishPrimarySurfaceBrush}" CornerRadius="12">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
              <flourish:FlourishTextBlock FontFamily="{DynamicResource FlourishIconFontFamily}" FontSize="{DynamicResource FlourishFontSizeIcon}" Foreground="{DynamicResource FlourishPrimaryForegroundBrush}" Role="Icon" Text="&#xE7C8;" />
              <flourish:FlourishTextBlock Margin="0,12,0,0" Role="CardTitle" Text="Presenter" />
            </StackPanel>
          </Border>
        </flourish:ChunkHero.Presenter>
      </flourish:ChunkHero>

      <flourish:Chunk ChunkTitle="Variant">
        <UniformGrid Columns="2">
          <flourish:Card Margin="0,0,8,0" Text="Places the presenter beside the hero content." Title="Split" />
          <flourish:Card Margin="8,0,0,0" Text="Layers the hero content over the presenter." Title="Overlay" />
        </UniformGrid>
      </flourish:Chunk>

      <flourish:Chunk ChunkTitle="Table">
        <flourish:DataGrid ItemsSource="{Binding Properties, ElementName=RootPage}" AutoGenerateColumns="False" IsReadOnly="True">
          <DataGrid.Columns>
            <DataGridTextColumn Width="220" Binding="{Binding Name}" Header="Property" />
            <DataGridTextColumn Width="*" Binding="{Binding Description}" Header="Purpose" />
          </DataGrid.Columns>
        </flourish:DataGrid>
      </flourish:Chunk>

      <flourish:Chunk ChunkTitle="Example">
        <UniformGrid Columns="2">
          <flourish:Card Margin="0,0,8,0">
            <flourish:Card.Body>
              <flourish:FlourishTextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{DynamicResource FlourishIconFontFamily}" FontSize="{DynamicResource FlourishFontSizeIcon}" Foreground="{DynamicResource FlourishAccentForegroundBrush}" Role="Icon" Text="&#xE8A5;" />
            </flourish:Card.Body>
          </flourish:Card>
          <flourish:Card Margin="8,0,0,0" Text="ChunkBody accepts controls, cards, lists, and custom layouts." />
        </UniformGrid>
      </flourish:Chunk>

      <flourish:Chunk ChunkTitle="Chunk content">
        <flourish:FlourishTextBlock Role="Paragraph" Text="ChunkTitle names one topic, ChunkDescription adds optional context, and ChunkBody hosts its content." />
      </flourish:Chunk>

      <flourish:Chunk ChunkTitle="Hero content">
        <flourish:FlourishTextBlock Role="Paragraph" Text="ChunkHero adds a presenter and reserves the page-leading position for one introduction." />
      </flourish:Chunk>

      <flourish:Chunk ChunkTitle="Usage">
        <flourish:FlourishTextBlock Role="Paragraph" Text="Start with at most one ChunkHero, then place each distinct topic in its own Chunk. Omit ChunkDescription when the title is sufficient." />
      </flourish:Chunk>

      <flourish:Chunk ChunkTitle="Reference">
        <UniformGrid Columns="2">
          <flourish:CardButton Margin="0,0,8,0" Content="Browse the Flourish source repository." Icon="&#xE8A5;" IsEnabled="False" Title="GitHub repository" ToolTip="External navigation is not available in Gallery." />
          <flourish:CardButton Margin="8,0,0,0" Content="Read the Chunk and ChunkHero guide." Icon="&#xE946;" IsEnabled="False" Title="Chunk documentation" ToolTip="External navigation is not available in Gallery." />
        </UniformGrid>
      </flourish:Chunk>
    </StackPanel>
  </flourish:ScrollViewer>
</Page>
